import {StyleSheet} from "react-native";
import {SHADOW} from "@/theme/shadow";

export const styles = StyleSheet.create({
    arrowContainer: {
        width: 48,
        height: 48,
        alignItems: "center",
        justifyContent: "center",
    },
    arrowShadow: SHADOW.large,
});
